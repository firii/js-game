class TileMap {
    constructor(width, height, tileset) {
        this._width = width;
        this._height = height;

        this._tileset = tileset;

        this.tiles = [];
        //tomorrow... well, today actually
    }
}